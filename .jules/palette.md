## 2024-05-23 - Dynamic titles for disabled buttons
**Learning:** Found that many disabled buttons in this app lacked an explanation for why they were disabled (e.g. no file to download, empty prompt).
**Action:** Adding dynamic `title` attributes (or replacing generic ones) on disabled buttons significantly improves accessibility and UX for screen reader and mouse users.
