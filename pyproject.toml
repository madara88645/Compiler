[project]
name = "promptc"
version = "2.0.45"
description = "Prompt Compiler App: compile free-text prompts into structured IR + prompts + plan"
authors = [ { name = "madara88645" } ]
requires-python = ">=3.10"
readme = "README.md"
license = { text = "Apache-2.0" }
dependencies = [
	"fastapi==0.128.0",
	"uvicorn==0.40.0",
	"pydantic==2.12.5",
	"typer==0.21.1",
	"click==8.3.1",
	"jsonschema==4.26.0",
	"rich==14.2.0",
	"textual==7.0.1",
	"pyyaml>=6.0",
	"ttkbootstrap>=1.10.1",
	"Jinja2>=3.1.0",
	"tiktoken>=0.7.0",
	"cachetools>=5.3.3",
	"sqlalchemy>=2.0.0",
	"python-dotenv>=1.0.0",
]

[tool.setuptools]
package-dir = {"" = "."}
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
exclude = ["tests*", "docs*", "examples*", "promptc.egg-info*"]

[tool.setuptools.package-data]
app = [
	"_schemas/*.json",
	"_builtin_templates/*.yaml",
	"_builtin_templates/*.yml",
]

[project.scripts]
promptc = "cli.main:app"

[project.optional-dependencies]
dev = [
	"pytest==9.0.2",
	"pytest-cov>=5.0.0",
	"build>=1.2.0",
	"ruff==0.14.10",
	"pre-commit==4.5.1",
	"httpx<0.29",
]

openai = [
	"openai>=1.30.0",
]

docs = [
	"pymupdf>=1.24.0",
	"python-docx>=1.1.0",
]

all = [
	"openai>=1.30.0",
	"pymupdf>=1.24.0",
	"python-docx>=1.1.0",
]

security = [
	"h11>=0.16.0",
	"anyio>=4.4.0",
	"zipp>=3.19.1",
	"pillow>=10.3.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
