{
  "name": "promptc-intent-compiler",
  "displayName": "PromptC Intent Compiler",
  "description": "Compile natural language intent into prompts, plans, and visible safety policy inside VS Code.",
  "version": "0.0.1",
  "publisher": "memo",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:promptc.compileSelection",
    "onCommand:promptc.openPanel"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "promptc.compileSelection",
        "title": "PromptC: Compile Selection"
      },
      {
        "command": "promptc.openPanel",
        "title": "PromptC: Open Intent Panel"
      }
    ],
    "configuration": {
      "title": "PromptC",
      "properties": {
        "promptc.apiBaseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:8080",
          "markdownDescription": "PromptC API base URL used by the VS Code extension."
        },
        "promptc.conservativeMode": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When enabled, PromptC asks for grounded, conservative output."
        }
      }
    }
  }
}
